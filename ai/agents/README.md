# ai/agents

This folder contains agent definitions — structured descriptions of autonomous or semi-autonomous LLM-driven actors. Each agent file specifies the agent's name, role, goals, constraints, and any tools or sub-agents it is allowed to invoke. Definitions are written in a model-agnostic format so they can be loaded by any orchestration framework without modification.
