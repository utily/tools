# Import Style

- Import from the package's top-level index when the repository convention uses it.
- Prefer stable public entrypoints over deep imports.
- Import the file index without extension so that debugging back to the source code works.
