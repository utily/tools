# automation/cli

This folder contains command-line interface (CLI) tools intended for direct use by developers. Each subdirectory or file represents a standalone CLI program with its own entry point and usage documentation. Prerequisites and installation steps for each tool are described in the tool's accompanying documentation or inline help output.
