# process/release

This folder contains the release workflow — the ordered sequence of steps required to prepare, validate, publish, and announce a new version of a project. It covers versioning conventions, changelog generation, artifact publishing, and post-release verification, and should be updated whenever the release process changes.
